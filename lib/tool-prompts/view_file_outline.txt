Return a structural outline of a source file (functions, classes, types with line/endLine) without loading the full body.
Uses AST parsing (brace-aware; optional tree-sitter when CHATRE_TREE_SITTER=1). Prefer before ranged read_file or patch_file.
