Return a structural outline of a source file (functions, classes, types, headings) with line numbers — without loading the full body into context.
Use before ranged read_file or patch_file so you edit the right region with minimal churn.
